import AgeCalculator from './../src/ageCalculator';

describe('AgeCalculator', function() {
  it('create new ageCalculator object', function() {
    var newAgeCalculator = new AgeCalculator();
    expect(newAgeCalculator.getAge).toEqual();
  });
});
